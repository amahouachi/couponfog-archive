import Message from "../Message";

/*
test('basic', () => {
  const m1= new Message(Message.ID_USER_CONNECTION_SUCCESS);
  expect(m1.id).toBe(Message.ID_USER_CONNECTION_SUCCESS);
  expect(m1.data).toBe(undefined);
  const m2= new Message(Message.ID_INJECT_SCRIPT, {type: "x"});
  expect(m2.id).toBe(Message.ID_INJECT_SCRIPT);
  expect(m2.data).toBeDefined();
  expect(m2.data.type).toBe("x");
});
*/
