//import 'isomorphic-fetch';

/*
beforeAll(async () => {
    await Config.initialize();
});

describe('config', () => {
    test('getSystembyUrl', async () => {
        const uptplus: System = Config.getSystemByUrl(UPTPLUS_HOME_URL);
        const sht: System = Config.getSystemByUrl(SHT_HOME_URL);
        expect(uptplus.name).toBe("uptplus");
        expect(sht.name).toBe("sht");
    });

    test('getSystemByName',async () => {
        const uptplus:System = Config.getSystemByName("uptplus");
        expect(uptplus.name).toBe("uptplus");
        expect(uptplus.session).not.toBe(undefined);
        expect(uptplus.form?.view).not.toBe(undefined);
    });

    test('getFormByUrl',async () => {
        const sht:System = Config.getSystemByName("sht");
        const viewFormName = Config.getFormByUrl(SHT_COMPONENT_VIEW_URL,sht);
        expect(viewFormName).toBe("view");
        const createFormName = Config.getFormByUrl(SHT_COMPONENT_CREATE_URL,sht);
        expect(createFormName).toBe("create");
    });
});
*/
